#!/bin/sh



cat << EOF
---
addons:
  - heroku-postgresql:hobby-dev
config_vars:
  PATH: /usr/bin:/bin:$HOME/.roswell/bin
  LANG: en_US.UTF-8
default_process_types:
  web: web: clackup --port $PORT app.lisp
EOF
